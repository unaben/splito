"use client";

import styles from "./SignoutButton.module.css";

const SignoutButton = () => {
  return (
    <div>
      <button onClick={() => {}} className={styles.signout}>
        Sign out
      </button>
    </div>
  );
};

export default SignoutButton;
